# Nacos 注册发现配置（替代 Consul）
# 启动方式：mvn spring-boot:run -Dspring-boot.run.profiles=nacos
spring:
  cloud:
    consul:
      enabled: false
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
